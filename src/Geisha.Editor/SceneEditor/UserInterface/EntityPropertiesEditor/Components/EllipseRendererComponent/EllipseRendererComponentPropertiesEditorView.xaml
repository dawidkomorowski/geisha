<UserControl
    x:Class="Geisha.Editor.SceneEditor.UserInterface.EntityPropertiesEditor.Components.EllipseRendererComponent.EllipseRendererComponentPropertiesEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:Geisha.Editor.SceneEditor.UserInterface.EntityPropertiesEditor.Components.EllipseRendererComponent"
    xmlns:controls="clr-namespace:Geisha.Editor.Core.Controls"
    xmlns:core="clr-namespace:Geisha.Editor.Core"
    xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="300" x:ClassModifier="internal"
    d:DataContext="{d:DesignInstance local:EllipseRendererComponentPropertiesEditorViewModel}">
    <UserControl.Resources>
        <core:ColorConverter x:Key="ColorConverter" />
    </UserControl.Resources>
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="5" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="10" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0">Radius X</TextBlock>
        <controls:DoubleInput Grid.Row="0" Grid.Column="2" Value="{Binding Path=RadiusX}" />

        <TextBlock Grid.Row="2" Grid.Column="0">Radius Y</TextBlock>
        <controls:DoubleInput Grid.Row="2" Grid.Column="2" Value="{Binding Path=RadiusY}" />

        <TextBlock Grid.Row="4" Grid.Column="0">Color</TextBlock>
        <xctk:ColorPicker Grid.Row="4" Grid.Column="2"
                          SelectedColor="{Binding Path=Color, Converter={StaticResource ColorConverter}}" />

        <TextBlock Grid.Row="6" Grid.Column="0">Fill interior</TextBlock>
        <CheckBox Grid.Row="6" Grid.Column="2" IsChecked="{Binding Path=FillInterior}" />

        <TextBlock Grid.Row="8" Grid.Column="0">Visible</TextBlock>
        <CheckBox Grid.Row="8" Grid.Column="2" IsChecked="{Binding Path=Visible}" />

        <TextBlock Grid.Row="10" Grid.Column="0">Sorting layer name</TextBlock>
        <TextBox Grid.Row="10" Grid.Column="2" Text="{Binding Path=SortingLayerName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Grid.Row="12" Grid.Column="0">Order in layer</TextBlock>
        <controls:IntInput Grid.Row="12" Grid.Column="2" Value="{Binding Path=OrderInLayer}" />
    </Grid>
</UserControl>