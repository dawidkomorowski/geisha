<UserControl x:Class="Geisha.Editor.Core.Views.DockableViews.ProjectExplorer.ProjectExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:projectExplorer="clr-namespace:Geisha.Editor.Core.ViewModels.DockableViews.ProjectExplorer"
             mc:Ignorable="d" d:DataContext="{d:DesignInstance projectExplorer:ProjectExplorerViewModel}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TreeView ItemsSource="{Binding Path=Items}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="projectExplorer:ProjectItemViewModel"
                                          ItemsSource="{Binding Path=Items}">
                    <TextBlock Text="{Binding Path=Name}" />
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>